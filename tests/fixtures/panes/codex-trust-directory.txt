# Captured verbatim from a live codex 0.144.4 TUI pane on first entry to a
# directory codex had not been trusted in. This blocks before any work starts,
# so a default-mode run stalls here unless the project is already trusted.
# The capture machine's path was replaced with /tmp/example; nothing else changed.

  Welcome to Codex, OpenAI's command-line coding agent
> You are in /tmp/example
  Do you trust the contents of this directory? Working with untrusted contents comes with higher risk of prompt injection. Trusting the directory allows project-local config, hooks, and exec policies
  to load.
› 1. Yes, continue
  2. No, quit
  Press enter to continue
